# FarmVista Beta — GitHub Configuration

This folder contains repository-level GitHub configuration and automation.

- `workflows/` contains GitHub Actions used by FarmVista Beta.
- Keep workflow YAML files inside `.github/workflows/` so GitHub Actions can detect and run them.

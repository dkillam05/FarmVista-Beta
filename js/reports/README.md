# Reports JavaScript

Report-specific client logic.

- `grain/` — Grain reports
- `operations/` — operational/non-Grain reports

Reusable source feature logic should remain with the source feature rather than being duplicated into Reports.

// FarmVista Beta — centralized Grain Operations renderer
import { startGrainWorkspace, onWorkspaceChange } from './workspace-controller.js';import { installWorkspaceInteractions } from './workspace-interactions.js';import { clean } from '../core/grain-rules.js';
const esc=v=>clean(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;'),bu=v=>Number(v||0).toLocaleString('en-US',{maximumFractionDigits:2});const date=v=>{const s=clean(v);if(!/^\d{4}-\d{2}-\d{2}/.test(s))return s||'—';const[y,m,d]=s.slice(0,10).split('-');return`${m}/${d}/${y}`},status=v=>`<span class="fv-go-status ${esc(clean(v).toLowerCase())}">${esc(clean(v)||'—')}</span>`;
function shell(root){root.innerHTML=`<section class="fv-go" aria-label="Grain Operations"><div class="fv-go-head"><div><h1 class="fv-go-title">Grain Contracts</h1><div class="fv-go-copy">Build hauling jobs, optionally link contract detail, assign grain tickets, and finish with settlement reconciliation.</div></div></div><section class="fv-go-workflow hauling"><div class="fv-go-workflow-head"><div><h2>Hauling Jobs</h2><p>Operational hauling plan. Hauling works independently; contracts can be linked later when detailed accounting is needed.</p></div><button class="fv-go-btn hauling" data-action="add-hauling">+ Add Hauling Job</button></div><div data-panel="hauling"></div></section><section class="fv-go-workflow contracts"><div class="fv-go-workflow-head"><div><h2>Grain Contracts &amp; Tickets</h2><p>Optional contract accounting. Existing hauling jobs and tickets remain operational whether or not contract detail is used.</p></div><div class="fv-go-actions"><a class="fv-go-btn secondary" href="/pages/reports/report-grain-contracts.html">View Report</a><button class="fv-go-btn" data-action="add-contract">+ Add Contract</button></div></div><div data-panel="contracts"></div><div data-panel="allocation"></div></section><section class="fv-go-workflow settlement"><div class="fv-go-workflow-head"><div><h2>Settlement</h2><p>Final reconciliation against buyer settlement sheets.</p></div></div><div data-panel="settlement"></div></section></section><div class="fv-go-modal" id="fv-go-detail-modal"><div class="fv-go-modal-card"><div class="fv-go-modal-head"><div class="fv-go-modal-title"></div><button class="fv-go-close" aria-label="Close">×</button></div><div class="fv-go-modal-body"></div></div></div>`;root.addEventListener('click',e=>{if(e.target.closest('.fv-go-close')||e.target.id==='fv-go-detail-modal')root.querySelector('#fv-go-detail-modal')?.classList.remove('open')})}
function haulingPanel(model){const rows=model.haulingJobs.map(j=>`<tr data-id="${esc(j.id)}" data-kind="hauling"><td>${status(j.effectiveStatus)}</td><td><strong>${esc(j.jobName||j.displayName||j.deliveryLocationName||'—')}</strong></td><td>${esc(j.buyerName||'—')}</td><td>${esc(j.deliveryLocationName||'—')}</td><td>${esc(j.customerName||j.soldUnder||'—')}</td><td>${esc(j.crop||j.commodity||'—')}</td><td>${bu(j.targetBushels)}</td><td>${bu(j.hauledBushels)}${j.overhaulBushels>0?` <span class="fv-go-over">(+${bu(j.overhaulBushels)})</span>`:''}</td><td>${bu(j.remainingBushels)}</td><td>${j.linkedContractCount||0}</td><td>${date(j.deliveryStartDate)} – ${date(j.deliveryEndDate)}</td></tr>`).join('');return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Hauling Jobs</h3><p>Click any hauling job to review tickets, edit it, or void it. Ticketed and remaining bushels are calculated from physical grain tickets.</p></div></div><div class="fv-go-compact"><span>Jobs <strong>${model.summary.haulingJobs}</strong></span><span>Starting <strong>${bu(model.summary.haulingStarting)}</strong></span><span>Ticketed <strong>${bu(model.summary.haulingTicketed)}</strong></span><span>Remaining <strong>${bu(model.summary.haulingRemaining)}</strong></span><span>Linked Contracts <strong>${model.haulingJobs.reduce((s,j)=>s+(j.linkedContractCount||0),0)}</strong></span></div><div class="fv-go-filters"><input data-filter="hauling-search" placeholder="Job, buyer, location, customer…"><select data-filter="hauling-status"><option value="">All Jobs</option><option>active</option><option>upcoming</option><option>completed</option><option>overhauled</option><option>closed</option></select><select data-filter="hauling-crop"><option value="">All Crops</option><option>Corn</option><option>Soybeans</option></select><button class="fv-go-btn secondary" data-action="clear-hauling">Clear Filters</button></div><div class="fv-go-table-wrap"><table class="fv-go-table"><thead><tr><th>Status</th><th>Hauling Job</th><th>Buyer</th><th>Location</th><th>Sold Under</th><th>Crop</th><th>Starting Bu.</th><th>Ticketed Bu.</th><th>Remaining</th><th>Contracts</th><th>Delivery</th></tr></thead><tbody>${rows||'<tr><td colspan="11" class="fv-go-empty">No hauling jobs.</td></tr>'}</tbody></table></div></div>`}
function contractsPanel(model){const rows=model.contracts.map(c=>`<tr data-id="${esc(c.id)}" data-kind="contract"><td>${status(c.effectiveStatus)}</td><td><strong>${esc(c.contractNumber||c.number||'—')}</strong></td><td>${esc(c.buyerName||'—')}</td><td>${esc(c.customerName||c.soldUnder||'—')}</td><td>${esc(c.crop||c.commodity||'—')}</td><td>${esc(c.contractType||c.type||'—')}</td><td>${bu(c.targetBushels)}</td><td>${bu(c.deliveredBushels)}</td><td>${bu(c.remainingBushels)}</td><td>${esc(c.haulingJobName||'—')}</td><td>${date(c.deliveryStartDate||c.deliveryStart)} – ${date(c.deliveryEndDate||c.deliveryEnd)}</td></tr>`).join('');return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Grain Contracts</h3><p>Delivered and open bushels come from tickets actually allocated to each contract.</p></div></div><div class="fv-go-compact"><span>Contracts <strong>${model.summary.contracts}</strong></span><span>Contracted <strong>${bu(model.summary.contractBushels)}</strong></span><span>Assigned <strong>${bu(model.summary.contractDelivered)}</strong></span><span>Open <strong>${bu(model.summary.contractRemaining)}</strong></span></div><div class="fv-go-filters"><input data-filter="contract-search" placeholder="Contract #, buyer, customer…"><select data-filter="contract-status"><option value="">All Contracts</option><option>pending</option><option>open</option><option>completed</option><option>overhauled</option><option>voided</option></select><select data-filter="contract-crop"><option value="">All Crops</option><option>Corn</option><option>Soybeans</option></select><button class="fv-go-btn secondary" data-action="clear-contract">Clear Filters</button></div><div class="fv-go-table-wrap"><table class="fv-go-table"><thead><tr><th>Status</th><th>Contract #</th><th>Buyer</th><th>Customer</th><th>Crop</th><th>Type</th><th>Contract Bu.</th><th>Assigned Bu.</th><th>Remaining</th><th>Hauling Job</th><th>Delivery</th></tr></thead><tbody>${rows||'<tr><td colspan="11" class="fv-go-empty">No grain contracts.</td></tr>'}</tbody></table></div></div>`}
function allocationPanel(model){const tickets=model.tickets.filter(t=>!t.isVoided).slice(0,100).map(t=>`<div class="fv-go-chip" draggable="true" data-ticket-id="${esc(t.id)}"><strong>${esc(t.ticketNumber||t.ticketNo||t.id)}</strong> · ${esc(t.crop||'—')} · ${bu(t.effectiveBushels)} bu${t.effectiveHaulingJobId?' · Hauling linked':''}</div>`).join(''),jobs=model.haulingJobs.filter(j=>['active','upcoming'].includes(j.effectiveStatus)).map(j=>`<div class="fv-go-chip" data-job-id="${esc(j.id)}"><strong>${esc(j.jobName||j.deliveryLocationName||'—')}</strong><br>${esc(j.crop||'—')} · ${esc(j.customerName||j.soldUnder||'—')} · ${j.spotLoadOnly?'Spot Loads':`${bu(j.remainingBushels)} remaining`}</div>`).join(''),contracts=model.contracts.filter(c=>!['voided','completed','overhauled'].includes(c.effectiveStatus)).map(c=>`<div class="fv-go-chip" data-contract-id="${esc(c.id)}"><strong>${esc(c.contractNumber||c.number||'Contract')}</strong><br>${esc(c.crop||'—')} · ${esc(c.customerName||c.soldUnder||'—')} · ${c.spotLoadOnly?'Spot Loads':`${bu(c.remainingBushels)} remaining`}</div>`).join('');return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Assign Grain Tickets</h3><p>Drag a ticket to a hauling job for the operational assignment, or to a contract for optional accounting detail. The two assignments remain independent.</p></div></div><div class="fv-go-dnd"><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Grain Tickets</div><div class="fv-go-dnd-body">${tickets||'<div class="fv-go-empty">No tickets.</div>'}</div></div><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Hauling Jobs</div><div class="fv-go-dnd-body">${jobs||'<div class="fv-go-empty">No hauling jobs available.</div>'}</div></div><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Optional Contract Detail</div><div class="fv-go-dnd-body">${contracts||'<div class="fv-go-empty">No contracts available.</div>'}</div></div></div></div>`}
function render(root,model,state){if(state?.error){root.querySelector('[data-panel="hauling"]').innerHTML=`<div class="fv-go-error">Unable to load Grain Operations: ${esc(state.error.message||state.error)}</div>`;return}root.querySelector('[data-panel="hauling"]').innerHTML=haulingPanel(model);root.querySelector('[data-panel="contracts"]').innerHTML=contractsPanel(model);root.querySelector('[data-panel="allocation"]').innerHTML=allocationPanel(model);root.querySelector('[data-panel="settlement"]').innerHTML='<div class="fv-go-block"><div class="fv-go-settlement"><strong>Settlement Reconciliation Comes Next</strong><div>Contract and ticket data are structured so settlement sheets can be matched here when this workflow is activated.</div><button class="fv-go-btn secondary" disabled>Import Settlement</button></div></div>'}
export async function mountGrainOperations(root){if(!root)throw new Error('Grain Operations mount element is required');if(!document.querySelector('link[data-fv-go-css]')){const link=document.createElement('link');link.rel='stylesheet';link.href='/js/grain/operations/ui/grain-operations.css?v=20260917-5';link.dataset.fvGoCss='1';document.head.appendChild(link)}shell(root);installWorkspaceInteractions(root);onWorkspaceChange((model,state)=>render(root,model,state));await startGrainWorkspace();return root}

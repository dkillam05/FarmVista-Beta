// FarmVista Beta — centralized Grain Operations renderer using the established page workflow.
import { startGrainWorkspace,onWorkspaceChange } from './workspace-controller.js';
import { installWorkspaceInteractions } from './workspace-interactions.js';
import { installPlanningLinkInteractions } from './planning-link-interactions.js';
import { openContractForm } from './form-dialogs.js';
import { clean } from '../core/grain-rules.js';
const esc=v=>clean(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;'),bu=v=>Number(v||0).toLocaleString('en-US',{maximumFractionDigits:2});
const date=v=>{const s=clean(v);if(!/^\d{4}-\d{2}-\d{2}/.test(s))return s||'—';const[y,m,d]=s.slice(0,10).split('-');return`${m}/${d}/${y}`};
const status=v=>{const raw=clean(v),key=raw.toLowerCase(),label=key==='closed'?'completed':raw;return`<span class="fv-go-status ${esc(key==='closed'?'completed':key)}">${esc(label||'—')}</span>`};
const unique=(rows,getter)=>[...new Map(rows.map(x=>{const v=clean(getter(x));return[v.toLowerCase(),v]}).filter(x=>x[0])).values()].sort((a,b)=>a.localeCompare(b));
const opts=values=>values.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');
const remainingClass=(remaining,target)=>{const r=Math.max(0,Number(remaining)||0),t=Math.max(0,Number(target)||0),pct=t>0?r/t:0;return pct<.05?'fv-go-remaining-low':pct<.35?'fv-go-remaining-mid':'fv-go-remaining-good'};
const remainingTitle=(remaining,target)=>{const r=Math.max(0,Number(remaining)||0),t=Math.max(0,Number(target)||0),pct=t>0?r/t*100:0;return pct.toLocaleString('en-US',{maximumFractionDigits:1})+'% remaining'};
const sortHead=(label,col,type='text')=>`<button type="button" class="fv-go-sort" data-sort-col="${col}" data-sort-type="${type}" aria-label="Sort by ${esc(label)}"><span>${esc(label)}</span><span class="fv-go-sort-bars" aria-hidden="true"><i></i><i></i><i></i></span></button>`;
const contractLedger=t=>(Array.isArray(t?.contractAllocations)?t.contractAllocations:[]).filter(a=>clean(a?.contractId)&&Number(a?.bushels)>0);
const allocatedContractBushels=t=>{const rows=contractLedger(t);return rows.length?rows.reduce((s,a)=>s+Math.max(0,Number(a.bushels)||0),0):(clean(t?.contractId)?Number(t?.effectiveBushels||0):0)};
const ticketContractBushels=(t,id)=>{const rows=contractLedger(t);if(rows.length)return rows.filter(a=>clean(a.contractId)===clean(id)).reduce((s,a)=>s+Math.max(0,Number(a.bushels)||0),0);return clean(t?.contractId)===clean(id)?Number(t?.effectiveBushels||0):0};
function shell(root){root.innerHTML=`<section class="fv-go" aria-label="Grain Operations"><div class="fv-go-head"><div><h1 class="fv-go-title">Grain Contracts</h1><div class="fv-go-copy">Build hauling jobs, link the contracts that belong to each job when needed, assign grain tickets to contracts, and finish with settlement reconciliation.</div></div></div><section class="fv-go-workflow hauling"><div class="fv-go-workflow-head"><div><h2>Hauling Jobs</h2><p>Operational hauling plan. Create the job, edit or void it when needed, and optionally link contracts to it.</p></div><button class="fv-go-btn hauling" data-action="add-hauling">+ Add Hauling Job</button></div><div data-panel="hauling"></div><details class="fv-go-collapse"><summary><span><strong>Assign Contracts to Hauling Jobs</strong><small>Optional contract planning</small></span><span class="fv-go-chevron" aria-hidden="true">⌄</span></summary><div data-panel="planning-links"></div></details></section><details class="fv-go-workflow contracts fv-go-workflow-collapse"><summary class="fv-go-workflow-head"><div><h2>Grain Contracts &amp; Tickets</h2><p>Contract accounting lives here. Review contracts first, then assign grain tickets to the matching contracts.</p></div><div class="fv-go-summary-actions"><span class="fv-go-chevron" aria-hidden="true">⌄</span></div></summary><div class="fv-go-workflow-actions"><a class="fv-go-btn secondary" href="/pages/reports/report-grain-contracts.html">View Report</a><button class="fv-go-btn" data-action="add-contract">+ Add Contract</button></div><div data-panel="contracts"></div><div data-panel="allocation"></div></details><details class="fv-go-workflow settlement fv-go-workflow-collapse"><summary class="fv-go-workflow-head"><div><h2>Settlement</h2><p>Final step: reconcile tickets and assigned contract bushels against the buyer settlement sheet.</p></div><span class="fv-go-chevron" aria-hidden="true">⌄</span></summary><div data-panel="settlement"></div></details></section><div class="fv-go-modal" id="fv-go-detail-modal"><div class="fv-go-modal-card"><div class="fv-go-modal-head"><div class="fv-go-modal-title"></div><button class="fv-go-close" data-dialog-close aria-label="Close">×</button></div><div class="fv-go-modal-body"></div></div></div>`;root.addEventListener('click',e=>{if(e.target.id==='fv-go-detail-modal')root.querySelector('#fv-go-detail-modal')?.classList.remove('open')})}
function haulingPanel(model){
  const rawJobs=model.haulingJobs.filter(j=>clean(j.effectiveStatus).toLowerCase()!=='voided');
  const statusKey=j=>{const s=clean(j.effectiveStatus).toLowerCase();return s==='overhauled'||s==='closed'?'completed':s};
  const completionDate=j=>clean(j.completedAt||j.completedDate||j.closedAt||j.updatedAt);
  const completionDay=j=>{const s=completionDate(j);const d=s?new Date(s):null;return d&&!Number.isNaN(d.getTime())?d:null};
  const recentCompleted=j=>{const d=completionDay(j);if(!d)return true;return Date.now()-d.getTime()<=14*86400000};
  const visibleJobs=rawJobs.filter(j=>statusKey(j)!=='completed'||recentCompleted(j));
  const rank={past_due:0,'past due':0,overdue:0,active:1,upcoming:2,completed:3};
  const time=v=>{const d=v?new Date(v):null;return d&&!Number.isNaN(d.getTime())?d.getTime():0};
  const defaultSort=(a,b)=>{const as=statusKey(a),bs=statusKey(b),ar=rank[as]??1,br=rank[bs]??1;if(ar!==br)return ar-br;return clean(a.jobName||a.displayName||a.deliveryLocationName).localeCompare(clean(b.jobName||b.displayName||b.deliveryLocationName),undefined,{numeric:true,sensitivity:'base'})};
  const displayJobs=[...visibleJobs].sort(defaultSort);
  let lastGroup='';const rows=displayJobs.map(j=>{const sk=statusKey(j),shownStatus=sk==='completed'?'completed':j.effectiveStatus,group=sk==='past due'?'Past Due Hauling Jobs':sk==='active'?'Active Hauling Jobs':sk==='upcoming'?'Upcoming Hauling Jobs':'Completed Hauling Jobs',header=group!==lastGroup?`<tr class="fv-go-group-row"><td colspan="11">${esc(group)}</td></tr>`:'';lastGroup=group;return header+`<tr data-id="${esc(j.id)}" data-kind="hauling"><td>${status(shownStatus)}</td><td><strong>${esc(j.jobName||j.displayName||j.deliveryLocationName||'—')}</strong></td><td>${esc(j.buyerName||'—')}</td><td>${esc(j.deliveryLocationName||'—')}</td><td>${esc(j.customerName||j.soldUnder||'—')}</td><td>${esc(j.crop||j.commodity||'—')}</td><td>${bu(j.targetBushels)}</td><td>${bu(j.hauledBushels)}${Number(j.targetBushels)>0&&j.overhaulBushels>0?` <span class="fv-go-over">(+${bu(j.overhaulBushels)})</span>`:''}</td><td><strong class="fv-go-remaining ${remainingClass(j.remainingBushels,j.targetBushels)}" title="${remainingTitle(j.remainingBushels,j.targetBushels)}">${bu(j.remainingBushels)}</strong></td><td>${j.linkedContractCount||0}</td><td>${date(j.deliveryStartDate)} – ${date(j.deliveryEndDate)}</td></tr>`}).join('');
  return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Hauling Jobs</h3><p>Click any hauling job to edit or void it. Starting bushels stay as the original target; ticketed and remaining come from physical tickets.</p></div></div><div class="fv-go-compact"><span>Jobs <strong>${model.summary.haulingJobs}</strong></span><span>Starting <strong>${bu(model.summary.haulingStarting)}</strong></span><span>Ticketed <strong>${bu(model.summary.haulingTicketed)}</strong></span><span>Remaining <strong>${bu(model.summary.haulingRemaining)}</strong></span><span>Linked Contracts <strong>${model.haulingJobs.reduce((s,j)=>s+(j.linkedContractCount||0),0)}</strong></span></div><div class="fv-go-filters five"><input data-filter="hauling-search" placeholder="Job, buyer, location, customer…"><select data-filter="hauling-status"><option value="">All Jobs</option><option>past due</option><option>active</option><option>upcoming</option><option value="completed">completed</option></select><select data-filter="hauling-crop"><option value="">All Crops</option><option>Corn</option><option>Soybeans</option></select><select data-filter="hauling-buyer"><option value="">All Buyers</option>${opts(unique(rawJobs,x=>x.buyerName))}</select><select data-filter="hauling-customer"><option value="">All Sold Under</option>${opts(unique(rawJobs,x=>x.customerName||x.soldUnder))}</select></div><div class="fv-go-table-wrap"><table class="fv-go-table"><thead><tr><th>${sortHead('Status',0)}</th><th>${sortHead('Hauling Job',1)}</th><th>${sortHead('Buyer',2)}</th><th>${sortHead('Location',3)}</th><th>${sortHead('Sold Under',4)}</th><th>${sortHead('Crop',5)}</th><th>${sortHead('Starting Bu.',6,'number')}</th><th>${sortHead('Ticketed Bu.',7,'number')}</th><th>${sortHead('Remaining',8,'number')}</th><th>${sortHead('Contracts',9,'number')}</th><th>${sortHead('Delivery',10,'date')}</th></tr></thead><tbody>${rows||'<tr><td colspan="11" class="fv-go-empty">No hauling jobs.</td></tr>'}</tbody></table></div></div>`}
function planningPanel(model){const activeContracts=model.contracts.filter(c=>c.effectiveStatus!=='voided'),unlinked=activeContracts.filter(c=>!clean(c.haulingJobId)),linked=activeContracts.filter(c=>clean(c.haulingJobId));const card=c=>`<div class="fv-go-planning-card" draggable="true" data-planning-contract-id="${esc(c.id)}"><strong>Contract ${esc(c.contractNumber||c.number||'—')}</strong><div>${esc(c.buyerName||'—')} · ${esc(c.customerName||c.soldUnder||'—')} · ${esc(c.crop||'—')}</div><small>${c.spotLoadOnly?'Spot Loads':`${bu(c.remainingBushels)} bu remaining`}</small></div>`;const cards=unlinked.map(card).join('');const jobs=model.haulingJobs.filter(j=>!['closed','voided'].includes(j.effectiveStatus)).map(j=>{const assigned=linked.filter(c=>clean(c.haulingJobId)===clean(j.id));return `<div class="fv-go-planning-drop" data-planning-job-id="${esc(j.id)}"><strong>${esc(j.jobName||j.deliveryLocationName||'—')}</strong><div>${esc(j.buyerName||'—')} · ${esc(j.crop||'—')} · ${esc(j.customerName||j.soldUnder||'—')}</div><small>${assigned.length} linked contract${assigned.length===1?'':'s'}</small>${assigned.length?`<div class="fv-go-planning-assigned">${assigned.map(card).join('')}</div>`:''}</div>`}).join('');return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Assign Contracts to Hauling Jobs</h3><p>Drag an unlinked contract onto the correct hauling job, or drag a linked contract back into Unlinked Contracts to remove its hauling-job planning link.</p></div></div><div class="fv-go-link-filters five"><input data-planning-filter="search" placeholder="Job, buyer, location, customer…"><select data-planning-filter="status"><option value="">Select Job Status</option><option>active</option><option>upcoming</option><option>completed</option></select><select data-planning-filter="crop"><option value="">Select Crop</option><option>Corn</option><option>Soybeans</option></select><select data-planning-filter="buyer"><option value="">Select Buyer</option>${opts(unique([...unlinked,...model.haulingJobs],x=>x.buyerName))}</select><select data-planning-filter="customer"><option value="">Select Sold Under</option>${opts(unique([...unlinked,...model.haulingJobs],x=>x.customerName||x.soldUnder))}</select></div><div class="fv-go-planning-message ready" data-planning-message>Drag contracts between Unlinked Contracts and Hauling Jobs.</div><div class="fv-go-two-col"><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Unlinked Contracts <span>${unlinked.length}</span></div><div class="fv-go-dnd-body" data-planning-unassign>${cards||'<div class="fv-go-empty">No unlinked contracts.</div>'}</div></div><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Hauling Jobs <span>${model.haulingJobs.filter(j=>!['closed','voided'].includes(j.effectiveStatus)).length}</span></div><div class="fv-go-dnd-body">${jobs||'<div class="fv-go-empty">No hauling jobs available.</div>'}</div></div></div></div>`}
function contractsPanel(model){
  const rawContracts=model.contracts.filter(c=>clean(c.effectiveStatus).toLowerCase()!=='voided');
  const statusKey=c=>{const s=clean(c.effectiveStatus).toLowerCase();return s==='overhauled'||s==='closed'?'completed':s};
  const rank={past_due:0,'past due':0,overdue:0,pending:1,open:1,active:1,upcoming:2,completed:3};
  const defaultSort=(a,b)=>{const ar=rank[statusKey(a)]??1,br=rank[statusKey(b)]??1;if(ar!==br)return ar-br;return clean(a.contractNumber||a.number||a.buyerName).localeCompare(clean(b.contractNumber||b.number||b.buyerName),undefined,{numeric:true,sensitivity:'base'})};
  const displayContracts=[...rawContracts].sort(defaultSort);
  let lastGroup='';
  const rows=displayContracts.map(c=>{const sk=statusKey(c),group=['past_due','past due','overdue'].includes(sk)?'Past Due Grain Contracts':sk==='upcoming'||sk==='pending'?'Upcoming Grain Contracts':sk==='completed'?'Completed Grain Contracts':'Active Grain Contracts',header=group!==lastGroup?`<tr class="fv-go-group-row"><td colspan="11">${esc(group)}</td></tr>`:'';lastGroup=group;return header+`<tr data-id="${esc(c.id)}" data-kind="contract"><td>${status(sk==='completed'?'completed':c.effectiveStatus)}</td><td><strong>${esc(c.contractNumber||c.number||'—')}</strong></td><td>${esc(c.buyerName||'—')}</td><td>${esc(c.customerName||c.soldUnder||'—')}</td><td>${esc(c.crop||c.commodity||'—')}</td><td>${esc(c.contractType||c.type||'—')}</td><td>${bu(c.targetBushels)}</td><td>${bu(c.deliveredBushels)}${Number(c.targetBushels)>0&&Number(c.deliveredBushels)>Number(c.targetBushels)?` <span class="fv-go-over">(+${bu(Number(c.deliveredBushels)-Number(c.targetBushels))})</span>`:''}</td><td><strong class="fv-go-remaining ${remainingClass(c.remainingBushels,c.targetBushels)}" title="${remainingTitle(c.remainingBushels,c.targetBushels)}">${bu(c.remainingBushels)}</strong></td><td>${esc(c.haulingJobName||'—')}</td><td>${date(c.deliveryStartDate||c.deliveryStart)} – ${date(c.deliveryEndDate||c.deliveryEnd)}</td></tr>`}).join('');
  return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Grain Contracts</h3><p>Click a contract to edit it. Delivered and open bushels come from tickets actually assigned to that contract.</p></div><label class="fv-go-show-voided"><input type="checkbox" data-action="show-voided-contracts"> Show voided</label></div><div class="fv-go-compact"><span>Contracts <strong>${model.summary.contracts}</strong></span><span>Contracted <strong>${bu(model.summary.contractBushels)}</strong></span><span>Assigned <strong>${bu(model.summary.contractDelivered)}</strong></span><span>Open <strong>${bu(model.summary.contractRemaining)}</strong></span></div><div class="fv-go-filters five"><input data-filter="contract-search" placeholder="Contract #, buyer, customer…"><select data-filter="contract-status"><option value="">All Contracts</option><option>past due</option><option>open</option><option>upcoming</option><option>completed</option></select><select data-filter="contract-crop"><option value="">All Crops</option><option>Corn</option><option>Soybeans</option></select><select data-filter="contract-buyer"><option value="">All Buyers</option>${opts(unique(model.contracts,x=>x.buyerName))}</select><select data-filter="contract-customer"><option value="">All Customers</option>${opts(unique(model.contracts,x=>x.customerName||x.soldUnder))}</select></div><div class="fv-go-table-wrap"><table class="fv-go-table"><thead><tr><th>${sortHead('Status',0)}</th><th>${sortHead('Contract #',1)}</th><th>${sortHead('Buyer',2)}</th><th>${sortHead('Sold Under',3)}</th><th>${sortHead('Crop',4)}</th><th>${sortHead('Type',5)}</th><th>${sortHead('Starting Bu.',6,'number')}</th><th>${sortHead('Ticketed Bu.',7,'number')}</th><th>${sortHead('Remaining',8,'number')}</th><th>${sortHead('Hauling Job',9)}</th><th>${sortHead('Delivery',10,'date')}</th></tr></thead><tbody>${rows||'<tr><td colspan="11" class="fv-go-empty">No grain contracts.</td></tr>'}</tbody></table></div></div>`}
function allocationPanel(model){const unallocated=model.tickets.filter(t=>!t.isVoided&&allocatedContractBushels(t)+.005<Number(t.effectiveBushels||0)),tickets=unallocated.slice(0,100).map(t=>{const assigned=allocatedContractBushels(t),remaining=Math.max(0,Number(t.effectiveBushels||0)-assigned);return`<div class="fv-go-ticket-card" draggable="true" data-ticket-id="${esc(t.id)}" data-ticket-detail="${esc(t.id)}"><div><strong>${esc(t.ticketNumber||t.ticketNo||t.id)}</strong><span>${bu(remaining)} bu</span></div><small>${esc(t.crop||'—')} · ${esc(t.buyerName||t.deliveryLocationName||'—')} · ${esc(t.customerName||t.soldUnder||'—')}${assigned>0?` · ${bu(assigned)} bu already assigned`:''}</small></div>`}).join('');const available=model.contracts.filter(c=>!['voided','completed','overhauled'].includes(c.effectiveStatus)),contracts=available.map(c=>{const assigned=model.tickets.map(t=>({t,bu:ticketContractBushels(t,c.id)})).filter(x=>!x.t.isVoided&&x.bu>0),assignedCards=assigned.map(({t,bu:amount})=>`<div class="fv-go-assigned-ticket" draggable="true" data-ticket-id="${esc(t.id)}" data-ticket-detail="${esc(t.id)}"><div><strong>${esc(t.ticketNumber||t.ticketNo||t.id)}</strong><span>${bu(amount)} bu</span></div><small>${esc(t.crop||'—')} · ${esc(t.customerName||t.soldUnder||'—')}</small></div>`).join('');return`<div class="fv-go-contract-drop" data-contract-id="${esc(c.id)}"><strong>Contract ${esc(c.contractNumber||c.number||'—')}</strong><div>${esc(c.crop||'—')} · ${esc(c.customerName||c.soldUnder||'—')}</div><small>${c.spotLoadOnly?'Spot Loads':`${bu(c.remainingBushels)} bu remaining`}</small>${assignedCards?`<div class="fv-go-assigned-section"><div class="fv-go-assigned-head">Assigned Tickets <span>${assigned.length}</span></div>${assignedCards}</div>`:''}</div>`}).join('');return `<div class="fv-go-block"><div class="fv-go-block-head"><div><h3>Assign Grain Tickets to Contracts</h3><p>Pick Buyer first, then Customer. Drag tickets onto matching crop contracts, drag assigned tickets back to Unassigned to undo, or move them directly to another matching contract. Hauling assignments remain independent.</p></div></div><div class="fv-go-reconcile-filters"><select data-reconcile="buyer"><option value="">Select Buyer</option>${opts(unique(model.contracts.filter(c=>c.effectiveStatus!=='voided'),x=>x.buyerName))}</select><select data-reconcile="customer" disabled><option value="">Select Customer</option></select><select data-reconcile="crop"><option value="">Select Crop</option><option>Corn</option><option>Soybeans</option></select></div><div class="fv-go-two-col"><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Unassigned Grain Tickets <span>${unallocated.length}</span></div><div class="fv-go-dnd-body" data-unassigned-ticket-list>${tickets||'<div class="fv-go-empty">No unassigned grain tickets.</div>'}</div></div><div class="fv-go-dnd-col"><div class="fv-go-dnd-head">Available Contracts <span>${available.length}</span></div><div class="fv-go-dnd-body" data-available-contract-list>${contracts||'<div class="fv-go-empty">No contracts available.</div>'}</div></div></div><div class="fv-go-unassign-drop" data-contract-unassign>Drop an assigned grain ticket here to return it to Unassigned.</div></div>`}
function settlementPanel(){return '<div class="fv-go-block"><div class="fv-go-settlement"><strong>Settlement Reconciliation Comes Next</strong><div>Contract and ticket data are structured so settlement sheets can be matched here.</div><button class="fv-go-btn secondary" disabled>Import Settlement</button></div></div>'}
function render(root,model,state){if(state?.error){root.querySelector('[data-panel="hauling"]').innerHTML=`<div class="fv-go-error">Unable to load Grain Operations: ${esc(state.error.message||state.error)}</div>`;return}root.querySelector('[data-panel="hauling"]').innerHTML=haulingPanel(model);root.querySelector('[data-panel="planning-links"]').innerHTML=planningPanel(model);root.querySelector('[data-panel="contracts"]').innerHTML=contractsPanel(model);root.querySelector('[data-panel="allocation"]').innerHTML=allocationPanel(model);root.querySelector('[data-panel="settlement"]').innerHTML=settlementPanel()}
export async function mountGrainOperations(root){if(!root)throw new Error('Grain Operations mount element is required');if(!document.querySelector('link[data-fv-go-css]')){const link=document.createElement('link');link.rel='stylesheet';link.href='/js/grain/operations/ui/grain-operations.css?v=20260918-12';link.dataset.fvGoCss='1';document.head.appendChild(link)}shell(root);installWorkspaceInteractions(root);installPlanningLinkInteractions(root);onWorkspaceChange((model,state)=>render(root,model,state));await startGrainWorkspace();const params=new URLSearchParams(location.search);if(params.get('addContract')==='1'){openContractForm(root);history.replaceState(null,'',location.pathname)}return root}

// FarmVista Grain Operations — hauling add/edit form model aligned to Firestore schema.
import { clean,number,round2 } from '../core/grain-rules.js';import { validateHaulingJob } from '../core/grain-validation.js';
export function haulingDraft(job={}){return{id:clean(job.id),buyerId:clean(job.buyerId),buyerName:clean(job.buyerName),deliveryLocationId:clean(job.deliveryLocationId),deliveryLocationName:clean(job.deliveryLocationName),crop:clean(job.crop??job.commodity),customerId:clean(job.customerId??job.grainCustomerId),grainCustomerId:clean(job.grainCustomerId??job.customerId),customerName:clean(job.customerName??job.soldUnder),startingBushels:round2(job.startingBushels??job.jobBushels??job.bushels),spotLoadOnly:job.spotLoadOnly===true,deliveryStartDate:clean(job.deliveryStartDate??job.startDate),deliveryEndDate:clean(job.deliveryEndDate??job.endDate),status:clean(job.status)||'active',active:job.active!==false,notes:clean(job.notes)}}
export function haulingPayload(draft){const data=haulingDraft(draft),startingBushels=Math.max(0,round2(number(data.startingBushels))),spotLoadOnly=startingBushels===0;const location=clean(data.deliveryLocationName),buyer=clean(data.buyerName),prefix=[buyer,location].filter((x,i,a)=>x&&a.indexOf(x)===i).join(' ');const jobName=spotLoadOnly?`${prefix} — Spot Loads`.trim():`${prefix} — ${Math.round(startingBushels).toLocaleString('en-US')} bu`.trim();return{buyerId:data.buyerId,buyerName:data.buyerName,deliveryLocationId:data.deliveryLocationId,deliveryLocationName:data.deliveryLocationName,customerId:data.customerId,customerName:data.customerName,crop:data.crop,commodity:data.crop,startingBushels,spotLoadOnly,deliveryStartDate:data.deliveryStartDate,deliveryEndDate:data.deliveryEndDate,displayName:jobName,jobName,active:data.active!==false,status:data.status||'active',notes:data.notes}}
export function validateHaulingDraft(draft){return validateHaulingJob(haulingPayload(draft))}

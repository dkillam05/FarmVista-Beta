// FarmVista Grain Operations — contract add/edit form model
import { clean, number, round2 } from '../core/grain-rules.js';
import { validateContract } from '../core/grain-validation.js';
export function contractDraft(contract={}){return{id:clean(contract.id),buyerId:clean(contract.buyerId??contract.deliveryLocationId),buyerName:clean(contract.buyerName??contract.deliveryLocationName),deliveryLocationId:clean(contract.deliveryLocationId??contract.buyerId),deliveryLocationName:clean(contract.deliveryLocationName??contract.buyerName),crop:clean(contract.crop??contract.commodity),customerId:clean(contract.customerId??contract.grainCustomerId),grainCustomerId:clean(contract.grainCustomerId??contract.customerId),customerName:clean(contract.customerName??contract.soldUnder),contractNumber:clean(contract.contractNumber??contract.number),contractType:clean(contract.contractType??contract.type)||'Cash',bushels:round2(contract.bushels??contract.contractBushels??contract.quantity),futuresPrice:number(contract.futuresPrice),basis:number(contract.basis),cashPrice:number(contract.cashPrice??contract.price),deliveryStartDate:clean(contract.deliveryStartDate??contract.startDate),deliveryEndDate:clean(contract.deliveryEndDate??contract.expirationDate??contract.endDate),status:clean(contract.status??contract.contractStatus)||'Open',notes:clean(contract.notes)}}
export function contractPayload(draft){const data=contractDraft(draft);data.bushels=Math.max(0,round2(data.bushels));data.commodity=data.crop;data.type=data.contractType;if(data.contractType==='Cash'||data.contractType==='Program'){data.futuresPrice=0;data.basis=0}return data}
export function validateContractDraft(draft){return validateContract(contractPayload(draft))}

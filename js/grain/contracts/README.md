# Grain Contracts

- root — contract list/status behavior
- `forms/` — add/edit contract form behavior
- `allocation/` — contract allocation/linking helpers
- `reports/` — contract-report client behavior

Hauling jobs remain a sibling Grain workflow, not a subfolder of Contracts, because FarmVista can use hauling jobs independently as part of simple grain tracking.

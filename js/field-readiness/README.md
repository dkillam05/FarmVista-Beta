# Field Readiness JavaScript

Field Readiness is a standalone FarmVista feature area for rainfall, weather, readiness formulas/thresholds, forecasts, ETA calculations and readiness mapping.

It is intentionally **not** a subfolder of `fields/`. `fields/` owns actual field management; this folder owns readiness/weather logic.
